package com.Banking_Application.config;

public class ApplicationConfig {

	/*
	 * @Bean public Docket api() { return new Docket(DocumentationType.SWAGGER_2)
	 * .apiInfo(apiInfo()) .select() .paths(PathSelectors.any()) .build(); }
	 * 
	 * private ApiInfo apiInfo() { return new
	 * ApiInfoBuilder().title("BANKING APPLICATION REST API")
	 * .description("API for Banking Application.") .version("1.0.0").build(); }
	 */
}